DEFINE_PROGRAM {
    VARIABLES {
        INTEGER {
            i;
        }

        FLOAT {
            total <- 0.0;
            total2 <- 0.0;
            total3 <- 0.0;
            j;
        };
    }

    IMPLEMENTATION {
        FOR i FROM 1 TO 5 {
            add(i, i);
        };
    }
}

DEFINE_FUNCTION add(a) {
    VARIABLES {
        STRING {a;}
    }

    IMPLEMENTATION {
        write("Ladies", " And ", "Gentlemen", " ", "WE GOTTEM");
    }
}